#In MusicTheory.ts

Quick check passed:

normalizeNote() exists.
generateScale() now uses normalizedRoot.
getNote() now uses normalizedRoot.
getChordNotes() now uses structured parsing (root + rawType).
Aliases are present (min*, dom*, '-' -> 'm').
Extended chords are present: 9, maj9, m9, 11, maj11, m11, 13, maj13, m13.
Octave handling for extensions is present (3 + Math.floor(interval / 12)).

scales map (major, minor, dorian, etc.)
It defines the legal pitch space. This is the core musical constraint layer that prevents random notes.

chordProgressions map
It gives reusable harmonic movement templates. This supports stylistic consistency and faster generation.

normalizeNote()
It standardizes note names (Bb, Eb, E#, Cb) into one internal format. This prevents parsing errors and improves cross-notation compatibility.

generateScale() using normalizedRoot
It ensures scales work even when users provide flats or enharmonic spellings. Without this, valid inputs fail unpredictably.

getNote() using normalizedRoot
It guarantees interval transposition is consistent for all root spellings. This is critical for chord building and progression logic.

New getChordNotes() parser regex
It separates root and chord type reliably (A#, Bb, m9, 13). This replaces fragile string stripping.

rawType cleanup (trim, remove spaces, lowercase)
It makes chord input robust to formatting differences (" min9 ", "Maj7"), improving user experience.

Alias map (min9 -> m9, dom11 -> 11, '-' -> 'm')
It supports multiple musician notations (jazz/chart style and text style) while keeping one canonical internal representation.

Added extended chord intervals (9, 11, 13, plus maj/m variants)
It enables richer harmony needed for Brazilian-funk and ambient color language, instead of only triads and sevenths.

Octave-aware extension voicing (3 + floor(interval/12))
It keeps 9th/11th/13th above the base chord, so voicings sound musical and clear instead of collapsed/muddy.

Safe fallback (intervals[type] || intervals[''])
Unknown chord labels still produce a valid major triad, so generation degrades gracefully instead of crashing.

generateArpeggio() depends on improved chord notes
Arpeggios now reflect extended harmony correctly, which improves melodic coherence in generated patterns.

